
/*#include <opencv2/opencv.hpp>
#include <iostream>

using namespace std;
using namespace cv;

int main() {
	Mat src;
	src = imread("d:/bash.png", IMREAD_GRAYSCALE);
	imshow("origin", src);

	Mat dst;
	dst = 255 - src; // 색상이 반전된다.
	imshow("reverse", dst);

	waitKey(0);
	return 0;
}
*/